https://codepen.io/naranoeur/pen/mMZOxB

https://codepen.io/nakyos/pen/EmXBYv

https://codepen.io/hilotacker/pen/ONZWoX

https://nnattawat.github.io/flip/

https://codepen.io/ory13/pen/vYYrmQo

background sound

display scoer with this : https://elements.envato.com/game-score-count-4-GMX2C6A

ombre table

commenter le code 

couleur du tableau par niveau

game duration

https://codepen.io/perryh/pen/JQZmxv



------------------------------------------------------


int fonction minimax (int depth)
{
   if (game over or depth = 0)
      return winning score or eval();
    
   int bestScore;
   move bestMove;

   if (nœud == MAX) { //=Programme
      bestScore = -INFINITY;
      for (each possible move m) {
         make move m;
         int score = minimax (depth - 1)
         unmake move m;
         if (score > bestScore) {
            bestScore = score;
            bestMove = m ;
         }
      }
   }
   else { //type MIN = adversaire
      bestScore = +INFINITY;
      for (each possible move m) {
         make move m;
         int score = minimax (depth - 1)
         unmake move m;
         if (score < bestScore) {
            bestScore = score;
            bestMove = m ;
         }
      }
   }
   return bestscore ;
}